image: ruby:latest

variables:
  JEKYLL_ENV: production
pages:
  stage: deploy
  script:
  - gem install jekyll
  - gem install nokogiri
  - bundle install
  - bundle exec jekyll build -d public
  artifacts:
    paths:
    - public
  only:
  - master

